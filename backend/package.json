{
  "name": "todo-app-backend",
  "version": "1.0.0",
  "description": "Minimal todo app backend — pipeline validation baseline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test 'test/**/*.test.js'",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.21.0",
    "pg": "^8.13.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "eslint": "^9.15.0"
  }
}
